# Features

This directory is the primary home for rebuilt ERP features in v2.

Each feature is expected to move toward:

- `domain/`: business rules
- `data/`: Supabase queries and persistence
- `ui/`: feature components and interaction logic

The current app code remains available elsewhere in the repo as a reference while we rebuild feature-by-feature.
